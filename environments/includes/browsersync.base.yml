version: "3.9"
services:
  php-fpm:
    ports:
      - ${BROWSERSYNC_PORT_WEB:-3000}:${BROWSERSYNC_PORT_WEB:-3000}
      - ${BROWSERSYNC_PORT_UI:-3001}:${BROWSERSYNC_PORT_UI:-3001}
    environment:
      - BROWSERSYNC_PORT_WEB=${BROWSERSYNC_PORT_WEB:-3000}
      - BROWSERSYNC_PORT_UI=${BROWSERSYNC_PORT_UI:-3001}